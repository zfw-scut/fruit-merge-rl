# 归档完整逐帧18维边128M新基线

## 修改原因

6号实例上的`sab-full-fall-edge18-128m-b16m-r1`已经完成正式训练和4096局30 FPS终评，
需要把可复现训练身份、关键权重、正式评估与遥测证据迁回本地，同时避免复制Replay和大量
冗余中间产物。

## 完成事实

- 父transition为128,001,280，旁路transition为16,777,216；
- 最终4096局30 FPS均分/中位分为6688.94/5123.5；
- 训练只有1局命中64水果技术容量，最终评估为0局；
- `best.pt`对应96.001536M的120 FPS准确里程碑，`latest.pt`和128M里程碑对应训练终点；
- 训练进程已退出；过时的`run_status.json`不作为完成证据。

## 迁移范围

白名单归档保留三份关键checkpoint、最终评估索引、训练/评估/监控/资源日志、训练曲线、
run身份、预热teacher身份和精确配置。Replay、缓存、启动失败产物及其余中间checkpoint
均未迁移。

归档SHA-256为
`db6456364f02103fa0d405fa4e7ee61b14e6a727e7b4cd65f8d850255a77daba`，本地逐文件校验
14个清单条目全部通过。校验后把过时的根目录`run_status.json`移至
`analysis/stale_run_status.json`，防止本地门户将0%快照误识别为活跃训练；原始压缩包仍
保留源文件布局。完整结果与证据边界见
`docs/model_evaluations/model-sab-full-fall-edge18-128m-b16m-r1.md`。

## 结果边界

本次只确认训练、终评和迁移完成。模型相对历史方案同时改变了初始化、物理、边输入、预算和
评估协议，不能把分差直接归因于取消快进、新边属性或旁路，也不能用单训练seed声明可复现
策略提升。当前不在本地运行CUDA评估。
