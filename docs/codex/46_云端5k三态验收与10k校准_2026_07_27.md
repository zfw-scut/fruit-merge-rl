# 云端 5k 三态验收与 10k 校准（2026-07-27）

## 训练语义身份

- 分支：`codex/work-1`
- 训练提交：重写后 `7e6cade2be1024b25963817a7e202bb1f91fa561`（重写前
  `368cc9bd554e237fd976786180afc112ad6f816e`）
- 正式配置 SHA-256：
  `08c35479ddabd1cb383cd2287a94a457ae951b80acef483c3177e64b37ed2541`
- 正式配置解析指纹：
  `6cceea5075e97f62938c8f4fa70690ebaf50253ea22b1f131271815dcbc95a64`
- 本地与云端全量测试：各 312/312 通过
- 云端正式 preflight：`ready=true`、无 warning、32/32 快照复演

## 5k v5 最终结论

有效运行：

```text
run:       runs/dqn_causal_smoke_5k_v5
control:   runs/cloud_stage_control/5k_v5_368cc9b
monitor:   runs/resource_monitor/dqn_causal_smoke_5k_v5_20260727T133958Z
readiness: runs/preflight/dqn_causal_smoke_5k_v5_readiness.json
```

运行从 update 0 开始，5000/5000 完成，env steps 46000。train、普通资源监控、
cgroup 监控和 readiness 退出码均为 0；readiness schema 2 返回
`ready=true`。

三态普通反事实最终账本：

```text
strict_match               147
numeric_jitter_drop          0
semantic_divergence_drop     1
unknown_failed               0
non_gate_result_failed       0
labels_ready               147
samples_inserted           255
semantic rate           0.6757%
```

唯一 semantic divergence 同时包含 `physics_result`、`final_state_semantics` 和
`fruit_semantics` 分叉，正确丢弃且没有生成标签。全窗口最大 actual/projected token
比例为 8.9842% / 9.0%，没有突破 10% 硬上限。

Shapley 的 observed/selected/completed/strict/samples 为
6383/3/3/3/9，failed、terminal drop、numeric 和 semantic 均为 0，样本真实进入
optimizer。snapshot failure 为 0，StateAnalyzer degraded 最大 0.1103%，shaping
p95 最大 0.05882，截断率 2/376 = 0.5319%。

最终 checkpoint 的 online/target、optimizer、CPU/CUDA RNG、manifest、hot TD replay
和精确因果 replay 均通过真实恢复检查。38 个 cold segment 连续，总计 5.045 GiB。

资源监控 656 个样本：CPU 平均 4.23/6 核、p95 5.32；GPU utilization 平均
10.41%、p95 27%，显存峰值 4166 MiB；cgroup working set 峰值 8.574 GiB，
low/high/max/OOM/OOM-kill 增量均为 0。

唯一非 required warning 是优雅关闭时清空 256 个未获批 top-K 候选；关闭后的
queued、inflight、reserved、pending 和 candidate pool 都为 0，不是任务或 token
泄漏。

## 10k 最终结论

10k 使用同一提交和冻结语义，从独立空目录 update 0 启动，没有 resume 或覆盖：

```text
run:     runs/dqn_causal_calibration_10k
control: runs/cloud_stage_control/10k_368cc9b
monitor: runs/resource_monitor/dqn_causal_calibration_10k_20260727T142156Z
start:   2026-07-27 22:21:56 +08:00
end:     2026-07-27 23:28:08 +08:00
readiness: runs/preflight/dqn_causal_calibration_10k_readiness.json
```

10000/10000 完成，env steps 92500；train、普通资源监控、cgroup 监控和 readiness
退出码均为 0，readiness schema 2 返回 `ready=true`。

最终三态账本为 strict/numeric/semantic = 283/1/1，总评估 285，semantic rate
0.3509%；`unknown_failed=0`、`non_gate_result_failed=0`，插入 522 个 CF 样本。
numeric jitter 首次提供了五类真实误差样本：

```text
merge-event position  0
fruit position        0.0017626
linear velocity       0.0013093
orientation           0.0028120
angular velocity      0.0000536
```

numeric 记录没有新增任何 `*_semantics` 或 `physics_result` 代码，
`label_ready=283` 严格等于 strict 数。semantic 记录则包含 `physics_result`、
`final_state_semantics` 和 `fruit_semantics`，两类 drop 均未生成标签。

全窗口最大 actual/projected token 比例为 9.0501%，hard budget 始终成立，
token overrun 为 0。Shapley observed/selected/completed/strict/samples 为
12985/6/6/6/18，failed、terminal drop、numeric 和 semantic 都为 0，样本进入
optimizer。正式配额已提供足够的 Shapley 链路证据，不再运行 25k。

checkpoint 的模型、optimizer、CPU/CUDA RNG、manifest、hot TD replay 和精确因果
replay 恢复全部通过。83 个 cold segment 连续，总计 11.186 GiB；最终因果 replay
包含 rule/CF/Shapley = 19502/480/18，正负 rule 各 9751。

StateAnalyzer degraded 最大 0.1102%，shaping p95 最大 0.05547；765 局中截断
6 局（0.7843%）。CPU 平均 4.38/6 核、p95 5.40；GPU utilization 平均 10.34%、
p95 25%，显存峰值 4118 MiB；cgroup working set 峰值 9.825 GiB，全部 memory
event 增量与 OOM/OOM-kill 均为 0。

唯一非 required warning 仍是优雅关闭时清空 256 个未获批 top-K 候选，占
12979 个 offer 的 1.9724%；关闭后的 queued、inflight、reserved、pending 和
candidate pool 都为 0。

## 磁盘边界

5k 后可用磁盘约 70 GiB，低于正式 500k preflight 的 80 GiB 门槛。10k 启动时为
69.984 GiB，采用 65 GiB 最低启动、45 GiB 优雅停止、40 GiB 硬停止的短跑门禁。
最终可用磁盘为 53.018 GiB；cold replay 与 checkpoint 分别占 11.186 GiB 和
5.775 GiB。运行全程没有触发 45/40 GiB 停止线。

该临时门禁只适用于已经获准的 10k 校准。正式 500k 之前必须恢复至少 80 GiB 可用
空间并重新运行 exact-commit preflight；不能通过降低正式阈值绕过。

## 当前批准边界

- 5k 与 10k 均已通过，不需要额外 25k。
- 完整状态归因、三态 CF、共享预算、Shapley、checkpoint 和资源稳定性已获得短跑证据。
- 云盘只剩 53.018 GiB，正式 500k 的 80 GiB 存储门禁未通过。
- 本文不批准启动 500k；需要扩盘，或取得历史产物迁移/删除的明确授权后重新 preflight。
