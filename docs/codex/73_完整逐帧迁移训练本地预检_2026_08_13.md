# 完整逐帧迁移训练本地预检

`SAB-FF120`（`sab-full-fall-t120-16m-r1`）的本地准备已通过：

- 目标TOML解析为120 FPS、16M transition、seed 20260813、完整逐帧下落；
- 本地RTX 4070 Laptop CUDA weights-only预检返回 `ready=true`；
- 来源 `best.pt` SHA-256为
  `fc05e5653981c600ecf313fe51c8e28d8cd033ebc419dd40d008a8d895a03ec2`；
- 来源checkpoint早于物理身份字段，报告为 `legacy_unspecified`；目标身份明确为
  `tensor_cuda_v3_full_fall`，只能weights-only初始化，不能resume；
- learner真实更新、target同步、Replay、双帧率短评估、checkpoint往返和曲线生成通过；
- 预检投影父Replay占用约4.19 GB；
- 正式训练入口的24-transition smoke正常完成2次update与最终评估，并生成
  `best.pt`、`final.pt`、`latest.pt`、身份文件和产物清单；
- smoke身份确认 `drop_fast_forward=false`，并记录target、optimizer、Replay、RNG、训练进度
  和epsilon日程进度全部重置。

预检报告为 `runs/preflight/sab_full_fall_t120_16m_r1_local.json`，smoke产物为
`runs/smoke/sab-full-fall-t120-16m-r1_local`。两者属于本地可执行性证据，不代表RTX 5090
吞吐或策略提升。服务器开启后必须重新执行同一预检，并在正式训练启动前检查目标run目录、
8765端口、来源哈希和固定代码身份。
