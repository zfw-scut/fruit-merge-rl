# 扩大堵塞风险监督规模并平衡训练

## 原因

首轮pilot使用512个CUDA环境采集426,496次投放，得到1,607个确认事件和54,592条
有效标签。它足以验证管线，但只有约当前128M基线父transition规模的0.33%，且逐等级
标签分布差异很大：模型容易使用等级先验代替同等级内部的几何风险判断。首轮整体测试
Average Precision不能作为模型已经可用的证据。

本阶段把真实确认事件目标扩大约30倍至50,000个，同时先修正训练贡献比例。扩量目标按
唯一确认事件计数，不按同一事件扩展出的连续标签行计数。

## 训练分布调整

- 数据仍按完整对局分组，禁止同一对局跨训练、验证和测试；划分时优先平衡拥有稀有等级
  确认事件的对局，验证和测试仍保留其自然样本比例；
- 训练不复制或删除数据行，而是令每个非空“等级×正负标签”单元拥有相同的总loss权重；
- 同一确认事件对应的连续正样本共享一次事件权重，避免持续时间较长的单个事件支配训练；
- 删除跨所有等级统一计算的`pos_weight`，验证和测试loss保持无权重自然分布；
- checkpoint选择从整体Average Precision改为逐等级有效Average Precision的宏平均，整体
  指标和逐等级指标仍同时记录。

这些权重只改变监督优化贡献，不改变原始标签、自然测试分布或模型输入。

## 事件与性能记录

- 采集manifest新增L7～L11实时确认事件计数；标签manifest新增逐等级、逐数据集的唯一
  确认事件数量；
- 新增`tools/benchmark_pair_risk_training.py`，短测多个batch，记录端到端samples/s、step
  时间和CUDA峰值显存；
- 标定选择距离最快吞吐20%以内的最小batch，以控制batch变化对优化行为的影响；
- 数据生成仍使用已验证的512环境配置。首轮120秒短测为3,551 env-step/s、GPU约100%，
  因此正式扩量不再额外扫描环境数量。

## 正式扩量计划

- teacher：登记的SAB-128；
- 物理：30 FPS完整逐帧、无快速自由落体；
- 并行环境：512；
- 目标确认事件：50,000；
- 单局投放上限：不设置；
- 标签窗口和检测阈值：保持首轮pilot配置；
- 训练batch：先在7号风险任务实例上使用pilot数据短测后确定；
- 数据生成完成后自动整理标签并训练同结构轻量模型。

本次扩量仍是独立风险代理训练，不接入Policy、当前128M基线或反事实旁路。只有独立测试
和人工检查证明同等级内部具有区分能力后，才讨论用于反事实候选筛选。

## 主要文件

- `src/daxigua/rl/pair_risk.py`
- `tools/generate_pair_risk_dataset.py`
- `tools/train_pair_risk.py`
- `tools/benchmark_pair_risk_training.py`
- `tests/test_pair_risk.py`

## 本地验证

- Python静态编译通过；
- `tests.test_pair_risk`共7项通过，覆盖稀有等级对局划分、等级/标签等总权重、正例事件
  去重复权重和原有模型、标签、训练端到端能力；
- 正式CUDA标定、50,000事件采集和模型训练结果以7号实例产物为准，不把启动或吞吐达标
  写成预测效果提升结论。

## 7号实例部署记录

实现提交`7ebf108`已部署到7号堵塞风险专用实例，6号128M基线训练实例未被修改。使用
首轮pilot数据完成RTX 5090训练batch标定：

| batch | samples/s | CUDA峰值分配 |
| ---: | ---: | ---: |
| 1,024 | 30,748 | 约68 MiB |
| 2,048 | 58,289 | 约117 MiB |
| 4,096 | 84,179 | 约218 MiB |
| 8,192 | 163,579 | 约420 MiB |
| 16,384 | 226,563～226,737 | 约823 MiB |
| 32,768 | 242,126 | 约1,627 MiB |

32,768仅比16,384快约6.9%，因此按“最快20%以内取最小batch”的规则选用16,384。

正式流水线使用512环境、50,000确认事件目标和`seed_base=92,000,000`启动。数据目录为
`/root/autodl-tmp/pair-risk/formal_50k_balanced_20260824`，训练产物目录为
`/root/autodl-tmp/pair-risk/model_50k_balanced_20260824`。启动稳定性检查时已采集966个确认
事件，逐等级计数为L7=43、L8=214、L9=464、L10=229、L11=16，GPU利用率97%，长局
混合吞吐约3,991 env-step/s。该记录只证明流水线正常运行；最终样本分布和模型效果等待
任务完成后评估。
