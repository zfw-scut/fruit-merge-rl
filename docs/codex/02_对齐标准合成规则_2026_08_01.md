# 对齐标准合成规则

## 修改原因

accelerated-v1 最小基线沿用了旧项目的水果生成范围、线性计分和终级水果不可合成
行为。经确认，本分支需要采用常见《合成大西瓜》规则中的水果顺序、前五级生成、
三角数计分、西瓜相消和合成中点，同时保留其余现有规则。

## 规则变化

- 新增 1–11 级名称；名称后来按仓库实际贴图修正为葡萄、樱桃、橘子、柠檬、
  猕猴桃、西红柿、桃子、菠萝、椰子、西瓜、大西瓜；
- 随机投放范围由 1–4 级扩展为 1–5 级，仍使用均匀随机；
- `merge_score(level)` 改为按两颗来源水果的等级计分，1–11 级依次为
  `1, 3, 6, 10, 15, 21, 28, 36, 45, 55, 66`；
- 两颗 11 级西瓜仍属于有效合成并获得 66 分；`is_mergeable_level(11)` 返回 `True`，
  `merge_target_level(11)` 返回 `None`，表示来源水果消失且不生成新水果；
- `merge_position()` 返回两个来源水果圆心的算术中点。
- 普通合成产生的新水果在创建瞬间线速度和角速度均为0；不继承两个来源水果的动量。

队列长度、各级半径、质量和投放/合成碰撞半径换算均保持不变。

## 主要文件

- `src/daxigua/core/rules.py`；
- `src/daxigua/core/__init__.py`；
- `tests/test_domain_contracts.py`。

## 旧实现审计

按 `docs/LEGACY_PROJECT_EXPERIENCE_INDEX.md` 只读检查了 `codex/work-1` 的
`core/rules.py`、`core/board.py` 和 `core/engine.py`。旧实现把合成水果放在较低来源
水果的圆心，只注册 1–10 级合成，并按合成后的等级计分。本次只在当前稳定领域契约
中重新实现已确认的规则，不复制旧模拟器。

## 验证

执行：

```powershell
$env:PYTHONPATH = 'src'
python -B -m unittest discover -s tests -v
```

10 项领域规则与状态契约测试全部通过。

## 贴图名称修正（2026-08-06）

本记录最初加入的“标准名称”没有核对仓库保留的 `assets/fruits/01.png～11.png`，
导致后续场景实验室和模型观看器出现名称与图案不一致。名称现按项目最早的水果类
`PT、YT、JZ、NM、MHT、XHS、TZ、BL、YZ、XG、DXG` 修正为：葡萄、樱桃、
橘子、柠檬、猕猴桃、西红柿、桃子、菠萝、椰子、西瓜、大西瓜。

本次只修正显示名称，不改变等级编号、半径、随机生成范围、合成关系或计分规则。

## 合成初速度修正（2026-08-11）

当前规则已明确改为：合成水果的创建位置仍是来源圆心中点，但创建瞬间的`vx/vy`和
`angular_velocity`全部置零。该规则覆盖2026-08-04引入的动量继承语义，详见
`53_合成水果初速度归零_2026_08_11.md`。
