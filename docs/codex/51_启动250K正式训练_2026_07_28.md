# 启动 250K 正式训练（2026-07-28）

## 结果

第一次结构感知 V2 正式训练已在新 RTX 5090 云服务器上从 update 0 启动，目标为
250000 updates。训练由独立 session supervisor 托管，SSH 或本地电脑断线不会终止
训练；普通资源监控、cgroup 内存监控和只读 HTML 面板均已绑定到本次 run。

## 启动门禁

- 本地和云端使用同一干净提交：重写后
  `610e12daa381f0c9a7262f778726206cbae86fdf`（重写前
  `83026cad5b43a8d39a7e224445f28091a5f9c785`）。
- 本地、云端各完成 376 项测试，0 failure/error；编译检查通过。
- 正式 preflight：15 项检查全部通过，`ready=true`，0 warning，
  EngineSnapshot 复现 32/32。
- 使用 `configs/train_dqn_causal_250k.toml` fresh start；没有 resume、长度覆盖或
  overwrite。

## 运行身份

- run：`runs/dqn_causal_structure_h256_l4_n3_250k`
- monitor：`runs/resource_monitor/formal_250k_83026ca_20260728T001851Z`
- control：`runs/stage_control/formal_250k_83026ca_20260728T001851Z`
- supervisor PID：`40568`
- 启动时间：`2026-07-28T08:18:51+08:00`

update 103 的稳定快照显示进度继续增加，面板数据新鲜，cgroup OOM 事件为 0，磁盘
仍有约 135 GiB。面板只读取训练已落盘数据，不进入训练计算图；它及 3 秒资源采样的
开销相对训练可忽略。
