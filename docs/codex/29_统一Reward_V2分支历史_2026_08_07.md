# 统一 Reward V2 分支历史

## 修改原因

第一版场景实验室在 `reward-v2` 上开发后被确认不符合实时操作预期，因此从较早提交分出
`scenario-lab-realtime` 重新实现。后续正确的场景实验室、Reward V2 正式训练和模型推理均在
新分支继续完成，导致分支名称与实际主线不一致。

## 分支调整

- 将废弃实现归档为 `archive/reward-v2-first-scenario-lab`；
- 使用保留当前代码树的合并提交接纳归档历史，不恢复废弃实现；
- 将重新实现后的正确主线统一为 `reward-v2`；
- 删除已被正式主线完整取代的远端 `scenario-lab-realtime`；
- 将首轮云端训练使用的 `8235ef9` 固定为 `model-reward-v2-r1` 注解标签。

## 保留边界

归档分支仅用于审计失败方案，不作为运行依赖，也不继续合并其中的场景实验室代码。
模型权重和训练大文件仍不进入 Git；标签只标识训练时使用的代码快照。

## 涉及文件

- `docs/GIT_WORKFLOW.md`；
- `docs/README.md`；
- `docs/codex/29_统一Reward_V2分支历史_2026_08_07.md`。

## 验证

- 确认合并提交相对正确主线父提交的文件差异为零；
- 确认远端 `reward-v2` 包含正确主线和废弃分支两侧历史；
- 确认远端归档分支和 `model-reward-v2-r1` 标签存在；
- 确认远端 `scenario-lab-realtime` 已删除。
