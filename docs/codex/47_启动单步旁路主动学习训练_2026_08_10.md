# 启动单步旁路主动学习训练

## 运行身份

- 云端提交：`bdf04ae1cbbaf8d1e5758e0430dc5e00b0da9ca0`；
- SSH入口：`root@connect.westc.seetacloud.com:18505`；
- GPU：NVIDIA GeForce RTX 5090，32607 MiB；
- 正式run：`runs/cloud_rtx5090_auxiliary_action_single_step_branch_bdf04ae_seed20260809_24m`；
- 启动PID：`11218`；
- 父训练预算：24M transition；
- 旁路预算：4M transition，来自1M个父决策状态，每个状态4个单步动作；
- 正式环境数：1792；父/旁路learner batch：256/64；旁路loss系数：0.25。

本页不保存登录密码。连接凭据由服务器实例管理；仓库文档只保留可审计的端点、提交和
运行身份。

## 云端CUDA门禁

正式训练前运行`tools/preflight_training.py`，结果为`ready=true`：

- 父DQN loss：0.003105；辅助loss：0.850647；
- 父/旁路联合loss：0.055874；旁路DQN loss：0.001515；
- 正式旁路读取样本占比：0.20；归一化有效loss占比：0.20；
- 父/旁路Replay容量投影合计：5220597760字节；
- checkpoint round-trip成功；
- 8局30 FPS和8局120 FPS短评估均完成；
- 未出现NaN、OOM、CUDA错误或Traceback。

预检文件为`runs/preflight/branch_bdf04ae.json`，日志为
`runs/preflight/branch_bdf04ae.log`。

## 面板与首批训练事实

面板绑定云端`127.0.0.1:8765`，本地已有SSH进程将其转发到
`http://127.0.0.1:8765/`。服务器端`/api/status`已返回训练事件、GPU资源历史和正式run
状态。训练结束后入口将保留最终只读面板。

首次稳定检查达到530944父transition：

- 更新1050次，epsilon为0.921188；
- 父Replay为530944/1048576；旁路Replay为0/262144；
- 父吞吐约2748.99 transition/s，预计父训练剩余约2.37小时；
- GPU利用率采样99%，显存约12220 MiB；
- 总loss 0.054050，DQN loss 0.032816，辅助loss 0.424681；
- bootstrap激活比例0.799219，与0.8配置一致；
- 旁路预算从6.4M父transition开始，因此当前旁路计数为0属于预期；
- 启动日志没有Traceback、OOM或非有限数值。

这些只是部署正确性和首批运行事实，不是模型效果结论。训练完成后必须迁回正式产物，
执行同评估seed的4096局30/120 FPS比较，并更新模型报告与比较矩阵。

## 完成与迁移

正式run已正常完成24,000,768父transition、4,000,000旁路transition和92,729次更新，
最终4096局30/120 FPS均分为4668.89/4347.91。完整归档已迁回本地，外层SHA-256为
`99bf4836c6bfbd4f64c0b2335daf0ea1736512dcc3f519679bd5c795872883e3`；内部manifest
23/23精确匹配。完整迁移与效果边界见`49_归档单步旁路主动学习训练_2026_08_10.md`和
`model_evaluations/model-auxiliary-action-single-step-branch-r4.md`。
