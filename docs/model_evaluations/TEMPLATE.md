# 模型评估：<模型代号>

## 1. 身份与证据

记录模型代号、Git 标签/提交、分支、奖励版本、模型结构、训练设备、seed、实际训练量、
run 路径、checkpoint 与指标文件 SHA-256。

## 2. 已确认效果

分别报告30 FPS和120 FPS的 episode 数、均值、中位数、标准差、投放数、合成数、最高等级、
终局水果数、危险投放率和技术超时率。每条结论使用 `【事实】`、`【观察】` 或 `【决策】`。

## 3. 已确认不足

只写有指标、回放或可复现场景支持的不足。无法复现的人工作用印象放入证据缺口。

## 4. 原因假设

| 假设 | 置信度 | 支持证据 | 反对证据 | 反证或验证方式 |
| --- | --- | --- | --- | --- |
| `<待填写>` | 低/中/中高/高 | `<待填写>` | `<待填写>` | `<待填写>` |

## 5. 下一轮最低实验

列出能区分不同原因假设的最小成对实验，不直接堆叠多个同时变化的功能。

## 6. 证据边界

说明训练 seed 数、评估样本、物理帧率、训练量差异、缺失产物以及不能由本轮证明的结论。
