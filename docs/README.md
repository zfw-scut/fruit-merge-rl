# 当前文档入口

> 当前阶段：**S6 — 完整逐帧物理域（2026-08-13，`codex/68–71`）**。
> 默认只读当前阶段及目标模块最近一次发生变化的阶段；不要按编号回读全部历史。

## 当前事实

- 合成水果创建瞬间线速度和角速度为零。
- 训练、评估和场景实验室共用 `TensorVectorSimulator` 与同一 CUDA Kernel；Pymunk 已退出当前运行依赖。
- 自由落体快进已从 Python/Tensor 与 CUDA 执行路径删除；训练、评估和场景实验室均逐帧模拟投放后的完整下落过程。
- 旧配置中的 `drop_fast_forward` 仅为读取兼容保留，传入 `true` 也会归一化为 `false`；旧物理身份的 checkpoint 不允许直接恢复训练，只能 weights-only 迁移。
- 场景实验室提供“Tensor/CPU vs CUDA后端”和“当前游玩 vs 当前训练配置”双环境实时对照。
- 门户统一承载文档、工具、训练遥测和场景实验室。
- 最新归档模型谱系为零初速度物理下的 128M 结构化辅助旁路模型及其 120 FPS weights-only 迁移模型。
- 更早模型、奖励和物理结果仍是历史证据，不是当前实现默认值。

## 按任务读取

| 任务 | 默认只读 |
| --- | --- |
| 场景实验室 | `SCENARIO_LAB.md` |
| 项目门户 | `PROJECT_PORTAL.md` |
| 模型或训练实现 | `model/AGENTS.md`，再选一份专项文档 |
| 模型效果或正式评估 | `model_evaluations/AGENTS.md`、比较矩阵的相关行、同谱系最新报告 |
| Git 操作 | `GIT_WORKFLOW.md` |
| 云服务器 | 本机 `CLOUD_SERVER_LOCAL.md` |
| 追溯修改原因 | `STAGE_INDEX.md`，再读命中的一份 `codex/` 记录 |
| 审计旧分支 | `LEGACY_PROJECT_EXPERIENCE_INDEX.md` |

## 新旧冲突处理

对“当前代码如何运行”，权威顺序为：

1. 当前代码、配置和测试；
2. 本页列出的当前操作文档；
3. 最新同谱系模型报告和比较矩阵；
4. `model/` 设计快照；
5. `codex/` 历史记录；
6. 旧分支经验。

旧文档中的“当前”“默认”“下一轮”只表示该文档写作时刻。若与更高优先级来源冲突，旧说法自动失效，不做折中合并。未来设计文档只描述候选方向，不代表已经实现。

完整阶段边界见 `STAGE_INDEX.md`。新增文档时优先更新当前入口或阶段索引，不再把所有历史文件平铺到本页。
