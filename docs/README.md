# 当前文档入口

> 当前阶段：**S7 — 无上限长局与空间状态研究（2026-08-14～17，`codex/76–83`）**。
> 默认只读当前阶段及目标模块最近一次发生变化的阶段；不要按编号回读全部历史。

## 当前事实

- 合成水果创建瞬间线速度和角速度为零。
- 训练、评估和场景实验室共用 `TensorVectorSimulator` 与同一 CUDA Kernel；Pymunk 已退出当前运行依赖。
- 自由落体快进已从 Python/Tensor 与 CUDA 执行路径删除；训练、评估和场景实验室均逐帧模拟投放后的完整下落过程。
- 当前稳定判定使用完整物理帧求解前后的圆心位移，并保留角速度条件；物理身份为
  `tensor_cuda_v4_solved_motion_stability`。
- 旧配置中的 `drop_fast_forward` 仅为读取兼容保留，传入 `true` 也会归一化为 `false`；旧物理身份的 checkpoint 不允许直接恢复训练，只能 weights-only 迁移。
- 正式训练默认不设置单局投放上限，并记录长局规模与水果槽占用；当前只监控，不强制终止、
  刷新或降权长期环境。
- 场景实验室提供“Tensor/CPU vs CUDA后端”和“当前游玩 vs 当前训练配置”双环境实时对照。
- 场景实验室提供完整加权 Voronoi 空间图参考可视化；该图尚未接入 observation、Replay
  或 Q 网络，不代表当前模型已使用 Space Graph。
- `SAB-128`在当前无快进环境中的 Merge Potential 20,000局正式采集和离线标签汇总已
  完成，全部为自然终局，正式数据已迁回本地；门户已提供时间尺度、单因素、双因素与原始
  表格分析页。
- 轻量逐水果合成步距预测器的全场景采集、离线标签、独立训练与验证链路已准备完成；
  三个阶段均已接入门户实时训练遥测；尚未在云端生成新训练数据或训练正式预测器，也未
  接入RL融合模型。
- 门户统一承载文档、工具、统计分析、训练遥测和场景实验室。
- 最新归档模型为完整逐帧120 FPS下的16M无旁路对照及从精确8M checkpoint分叉的2.097M旁路续训模型；最终结果不升级旁路版。
- Android游戏已合并到当前分支并接入无旁路`SAB-FF120 final.pt`；其Java物理不再包含逐水果运动学休眠。
- 更早模型、奖励和物理结果仍是历史证据，不是当前实现默认值。

## 按任务读取

| 任务 | 默认只读 |
| --- | --- |
| 场景实验室 | `SCENARIO_LAB.md` |
| 项目门户 | `PROJECT_PORTAL.md` |
| 模型或训练实现 | `model/AGENTS.md`，再选一份专项文档 |
| 模型效果或正式评估 | `model_evaluations/AGENTS.md`、比较矩阵的相关行、同谱系最新报告 |
| Merge Potential 数据 | `MERGE_POTENTIAL_DATA.md` |
| 合成步距预测器 | `model/MERGE_DISTANCE_PREDICTOR.md` |
| Git 操作 | `GIT_WORKFLOW.md` |
| 云服务器 | 本机 `CLOUD_SERVER_LOCAL.md` |
| 追溯修改原因 | `STAGE_INDEX.md`，再读命中的一份 `codex/` 记录 |
| 审计旧分支 | `LEGACY_PROJECT_EXPERIENCE_INDEX.md` |

## 新旧冲突处理

对“当前代码如何运行”，权威顺序为：

1. 当前代码、配置和测试；
2. 本页列出的当前操作文档；
3. 最新同谱系模型报告和比较矩阵；
4. `model/` 设计快照；
5. `codex/` 历史记录；
6. 旧分支经验。

旧文档中的“当前”“默认”“下一轮”只表示该文档写作时刻。若与更高优先级来源冲突，旧说法自动失效，不做折中合并。未来设计文档只描述候选方向，不代表已经实现。

完整阶段边界见 `STAGE_INDEX.md`。新增文档时优先更新当前入口或阶段索引，不再把所有历史文件平铺到本页。
