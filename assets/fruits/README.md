# 水果回放贴图

本目录包含等级 1 到 11 的透明 PNG，文件名固定为 `01.png` 到 `11.png`。
它们来自本项目共同基线中已经使用的游戏表现资源，当前分支仅复用这组小型纹理，
不依赖旧分支、旧工作树或旧渲染代码。

回放生成器会把图片转换为 `data:` URL 并嵌入单个 HTML，因此生成后的回放可以脱机
移动和打开。水果贴图按 `rules.py` 的显示半径绘制；可选碰撞圈仍按追踪记录中的真实
物理半径绘制，两种半径不得互相替代。
